# 微信完成通知规则（WeChat Notify · 独立工程）

当用户要求「任务/对话/项目跑完后用微信通知我」（或等价说法）时，agent 必须遵守：

## 执行要求

1. 在任务**收尾时**（无论成功还是失败都必须执行）运行：
   ```
   python "{{TOOL_DIR}}/wxnotify.py" "<结果摘要>" -t "<任务名>"
   ```
2. 结果摘要须包含：任务名、成功/失败、关键结果或错误摘要（简短，一两句话）
3. 渠道与限制：默认 ClawBot 渠道；微信限制**每下发 10 次或每 24 小时**需用户主动与 ClawBot 对话一次，
   否则下发失败——**脚本已内置自动兜底**：ClawBot 失败时自动切服务号（wechat，无保活限制）重发，
   无需 agent 手动切换。若需指定渠道：`-c wechat` / `-c clawbot`
4. 若环境中存在 MCP 工具 `send_wechat_message`，也可直接调用（等价）
5. 只在任务完成或失败时通知一次，不要每个中间步骤都通知

## 工具说明（无需用户再提供任何配置）

- 发送：`{{TOOL_DIR}}/wxnotify.py`（位置参数=内容；`-t` 标题；`-c` 渠道；`--dry-run` 预览）
- 接收：`{{TOOL_DIR}}/wxlisten.py`（`--wait` 等待 / `--once` 拉取 / `--status` 自检）
- 测试：`{{TOOL_DIR}}/wxtest.py`（端到端自检 + 微信回环）
- 配置：`token.txt`（用户 token）、`gate.env`（PushPlus 开放接口密钥）——均在本工程目录，勿外传
- 成功判据：输出 `OK: 服务端已接收`（code=200，异步投递，以手机收到为准）

## 双通道问答（提问时可选增强）

1. 推送问题：`python "{{TOOL_DIR}}/wxnotify.py" "<问题+选项>" -t "提问"`
2. 启动纯代码监听：`python "{{TOOL_DIR}}/wxlisten.py" --wait --timeout 30`（后台运行）
3. 告知用户：可直接回复，也可去微信 ClawBot 回复
4. 结束条件（任一满足即停）：前端回复 → 主动结束监听；微信回复 → 监听自动退出（exit 0），
   消息落盘 `inbox/`，读取最新一条作为用户回答
5. 注意：微信 `getMsg` 为拉取即消费队列，拉到即落盘；若 ClawBot 因保活失效，用户仍可随时发消息（收方向不受限制）

## 测试例程（用户说「运行测试例程」时执行）

运行 `python "{{TOOL_DIR}}/wxtest.py"`，它会检查配置 → 连通性/绑定 → 发送测试 → 等微信回复 ok 回环，最后输出 PASS/FAIL 汇总。会真发消息，属预期行为。
