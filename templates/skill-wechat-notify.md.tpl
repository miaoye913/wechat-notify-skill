---
name: wechat-notify
description: 给用户微信/飞书发通知消息（任务完成/失败/提醒），并支持双通道问答与测试例程。当用户要求"跑完/完成后用微信或飞书通知我"、"运行测试例程"时使用。默认走 notify.py（飞书优先、微信自动兜底）。凭证已配置于工具目录 {{TOOL_DIR}}，无需用户提供。
---

# 微信/飞书完成通知（Notify）

当用户要求「任务/对话/项目跑完后用微信/飞书通知我」（或等价说法）时，agent 必须遵守：

## 执行要求

1. 在任务**收尾时**（无论成功还是失败都必须执行）运行统一入口：
   ```
   python "{{TOOL_DIR}}/notify.py" "<结果摘要>" -t "<任务名>"
   ```
   该入口自动按「**飞书优先 → 微信 ClawBot → 微信服务号**」顺序投递（飞书无 24h 保活限制；微信链路内置兜底）。
2. 结果摘要须包含：任务名、成功/失败、关键结果或错误摘要（简短）
3. 需要指定通道时加 `--channel clawbot|wechat|feishu`；也可直接用底层脚本 `wxnotify.py` / `feishu_send.py`
4. 若环境中存在 MCP 工具 `send_wechat_message`，也可直接调用（等价）
5. 不要每个中间步骤都通知；只在任务完成或失败时通知一次

## 工具说明

- 统一入口：`{{TOOL_DIR}}/notify.py`；底层：`feishu_send.py`（飞书 REST，零依赖）、`wxnotify.py`（微信，默认 ClawBot，失败自动切服务号）
- 凭证位于工具同目录（`feishu.env` / `gate.env` / `token.txt`）；不要输出/展示密钥

## 双通道问答（向用户提问时可选增强）

1. 先把问题推送出去：`python "{{TOOL_DIR}}/notify.py" "<问题+选项>" -t "提问"`
2. 再启动纯代码监听（优先飞书，无保活限制）：
   - `python "{{TOOL_DIR}}/feishu_listen.py" --wait --timeout 30`（后台）
   - 备选：`python "{{TOOL_DIR}}/wxlisten.py" --wait --timeout 30`
3. 告知用户：可直接回复，也可去飞书/微信回复
4. 结束条件（任一满足即停）：用户前端回复 → 主动结束监听；用户飞书/微信回复 → 监听自动退出（exit 0），消息落盘 `inbox/`，读取最新一条作为回答
5. 处理后如有必要，用 notify.py 回发结果

## 测试例程（用户说「运行测试例程」时执行）

1. 运行：`python "{{TOOL_DIR}}/wxtest.py"`
2. 自动完成：配置检查 → 微信连通/绑定 → 微信发送测试 → 飞书凭据检查与发送测试 → 等微信回复 ok 的回环验证
3. 把 PASS/FAIL 汇总报告给用户；有 FAIL 按输出线索排查
4. 会真发消息，属预期行为；不要在用户不知情时运行
