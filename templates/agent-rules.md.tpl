# 微信/飞书完成通知规则（Notify）

当用户要求「任务/对话/项目跑完后用微信/飞书通知我」（或等价说法）时，agent 必须遵守：

## 执行要求

1. 在任务**收尾时**（无论成功还是失败都必须执行）运行统一入口：
   ```
   python "{{TOOL_DIR}}/notify.py" "<结果摘要>" -t "<任务名>"
   ```
   该入口自动按「**飞书优先 → 微信 ClawBot → 微信服务号**」顺序投递：
   飞书无 24h 保活限制；微信链路已内置保活失败的自动兜底，无需 agent 手动切换。
2. 结果摘要须包含：任务名、成功/失败、关键结果或错误摘要（简短，一两句话）
3. 若需要**指定通道**（例如只发微信）：
   ```
   python "{{TOOL_DIR}}/notify.py" "<结果摘要>" -t "<任务名>" --channel clawbot
   ```
   也可直接调用底层脚本：`wxnotify.py`（微信，默认 clawbot，失败自动切服务号）、`feishu_send.py`（飞书）
4. 若环境中存在 MCP 工具 `send_wechat_message`（微信通知 MCP），也可直接调用它，效果等价
5. 不要每个中间步骤都通知；只在任务完成或失败时通知一次

## 工具说明（供 agent 参考，无需用户再提供任何配置）

- 统一入口：`{{TOOL_DIR}}/notify.py`（飞书优先 + 微信兜底）
- 通道脚本：`{{TOOL_DIR}}/feishu_send.py`（飞书 REST，零依赖）、`{{TOOL_DIR}}/wxnotify.py`（微信，默认 ClawBot，失败自动切服务号）
- 凭证已配置在工具同目录（`token.txt` / `gate.env` / `feishu.env`），不需要用户提供；不要输出/展示任何密钥
- 返回 `OK:` 或 `=> 已通过【飞书】发送` 即成功；微信渠道为异步投递，以手机实际收到为准

## 双通道问答（向用户提问时可选增强）

需要向用户提问、且用户可能不在电脑前（或用手机更方便）时：

1. 先把**问题本身推送出去**（否则用户看不到问题）：
   `python "{{TOOL_DIR}}/notify.py" "<问题+选项>" -t "提问"`
2. 再启动**纯代码监听**（不做 AI 处理，只检测用户回复），按优先级选通道：
   - 飞书（推荐，无保活限制）：`python "{{TOOL_DIR}}/feishu_listen.py" --wait --timeout 30`（后台运行）
   - 微信（备选）：`python "{{TOOL_DIR}}/wxlisten.py" --wait --timeout 30`（后台运行）
3. 告知用户：可直接回复，也可去飞书/微信回复
4. 两个结束条件，满足即停：
   - 用户在前端回复 → 主动结束监听进程
   - 用户在飞书/微信回复 → 监听检测到消息**自动退出**（exit 0），消息已落盘工具目录下 `inbox/`，读取最新一条作为用户回答
5. 处理完后如有必要，用 notify.py 把结果回发

注意：飞书监听依赖 `lark-oapi`（`python -m pip install lark-oapi`）与同目录 `feishu.env`；微信监听依赖 PushPlus 开放接口密钥（同目录 `gate.env`）。微信 `getMsg` 是拉取即消费队列，拉到即落盘。

## 测试例程（用户说「运行测试例程 / 测试一下通知能不能用」时执行）

1. 运行端到端自检：
   `python "{{TOOL_DIR}}/wxtest.py"`
2. 它会自动完成：① 配置检查（微信 token/密钥、飞书凭证）② 微信连通性与 ClawBot 绑定 ③ 微信发送测试消息 ④ 飞书凭据检查与发送测试消息 ⑤ 等用户微信回复 ok 完成回环（默认 90 秒，可 `--timeout N`）
3. 把 PASS/FAIL 汇总报告给用户；有 FAIL 项按输出线索排查（密钥、安全 IP、绑定、24h 保活、飞书权限）
4. 测试会真发消息，属预期行为；不要在用户不知情时运行
